---
title: Entity-Relationship Diagram (ERD) Uitleg
description: Uitleg van de tabellen die laravel genereert.
---

import {LinkCard, Aside} from "@astrojs/starlight/components";

## Tabeloverzicht

### Jobs Tabel
De `jobs` tabel bevat informatie over taken die in het systeem worden uitgevoerd.

- **Kolommen**:
- `id`: Integer, de primaire sleutel van de tabel.
- `queue`: String, de naam van de wachtrij waarop de taak staat.
- `payload`: LongText, de JSON-gecodeerde gegevens van de taak.
- `attempts`: Integer, het aantal pogingen om de taak uit te voeren.
- `reserved_at`: Integer, timestamp voor wanneer de taak gereserveerd is.
- `available_at`: Integer, timestamp voor wanneer de taak beschikbaar is.
- `created_at`: Integer, timestamp voor wanneer de taak is aangemaakt.

### Cache Tabel
De `cache` tabel bevat informatie over batches van taken, mogelijk voor het groeperen en beheren van meerdere taken tegelijk.

- **Kolommen**:
- `key`: String, de sleutel van de cache.
- `value`: Text, de waarde van de cache.
- `expiration`: Integer, timestamp voor wanneer de cache verloopt.

### Cache-locks Tabel
De `cache_locks` tabel bevat informatie over cache locks, die mogelijk worden gebruikt om concurrentieproblemen bij toegang tot cachegegevens te voorkomen.

- **Kolommen**:
- `key`: String, de sleutel van de lock.
- `owner`: String, de eigenaar van de lock.
- `expiration`: Integer, timestamp voor wanneer de lock verloopt.

### Job-batches Tabel
De `job_batches` tabel bevat informatie over batches van taken, mogelijk voor het groeperen en beheren van meerdere taken tegelijk.

- **Kolommen**:
- `id`: String, de primaire sleutel van de tabel.
- `name`: String, de naam van de batch.
- `total_jobs`: Integer, het totale aantal taken in de batch.
- `pending_jobs`: Integer, het aantal wachtende taken in de batch.
- `failed_jobs`: Integer, het aantal mislukte taken in de batch.
- `failed_job_ids`: Text, de IDs van de mislukte taken in de batch.
- `options`: Text, de opties voor de batch in JSON-formaat.
- `created_at`: Timestamp, wanneer de batch is aangemaakt.
- `cancelled_at`: Timestamp, wanneer de batch is geannuleerd.
- `finished_at`: Timestamp, wanneer de batch is voltooid.

#### Failed_jobs Tabel
De `failed_jobs` tabel bevat informatie over mislukte taken in het systeem.

- **Kolommen**:
- `id`: Integer, de primaire sleutel van de tabel.
- `connection`: String, de naam van de verbinding.
- `queue`: String, de naam van de wachtrij.
- `payload`: LongText, de JSON-gecodeerde gegevens van de taak.
- `exception`: LongText, de uitzondering die de mislukking veroorzaakte.
- `failed_at`: Timestamp, wanneer de taak is mislukt.

### Sessions Tabel
De `sessions` tabel bevat sessie-informatie voor gebruikers die zijn ingelogd op het systeem.

- **Kolommen**:
- `id`: String, de primaire sleutel van de tabel.
- `user_id`: Integer, de ID van de gebruiker.
- `ip_address`: String, het IP-adres van de gebruiker.
- `user_agent`: Text, de user agent van de gebruiker.
- `payload`: Text, de sessiegegevens.
- `last_activity`: Integer, de laatste activiteitstijd.

### Personal_access_tokens Tabel
De `personal_access_tokens` tabel bevat persoonlijke toegangstokens voor gebruikers, mogelijk voor API-toegang.

- **Kolommen**:
- `id`: Integer, de primaire sleutel van de tabel.
- `tokenable_type`: String, het type van het token.
- `tokenable_id`: Integer, de ID van het token.
- `name`: String, de naam van het token.
- `token`: String, de hash van het token.
- `abilities`: Text, de mogelijkheden van het token.
- `last_used_at`: Timestamp, wanneer het token voor het laatst is gebruikt.
- `created_at`: Timestamp, wanneer het token is aangemaakt.
- `updated_at`: Timestamp, wanneer het token is bijgewerkt.